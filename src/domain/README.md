# domain/ — pure TypeScript

No `next/*`, no `react`, no vendor SDK, no `fetch`. Enforced by `eslint.config.mjs`.
Populated in MS4+ (`types.ts`, `errors.ts`, `ports.ts`, `import/pipeline.ts`) per `docs/07-import-execution-model.md` §10.
