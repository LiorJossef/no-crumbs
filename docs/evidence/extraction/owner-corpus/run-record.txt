
====================================================================================================
OWNER CORPUS — the engine, graded
====================================================================================================

   12 case(s): 1 labelled by the owner, 10 carrying the borrowed E7 labels, 1 still draft.
   Draft rows are printed above by name and count toward nothing.


----------------------------------------------------------------------------------------------------
SAMPLE: owner — the project owner's own saved TikToks. The distribution that matches the product.
----------------------------------------------------------------------------------------------------

   !! NOT YET A MEASUREMENT: 1 adjudicated case(s) in this sample. The document this
      corpus replaces had n=16 and a wide confidence interval; a smaller n does not fix that.

   cases in this sample: 1

   PER CLASS
   class          posts  graded  place-graded  yielded>=1 correct   wrong preselect  wrong shortlist  extraction misses  intent
   sufficient       0       0             0           0/0 (n/a)                0                0                  0      0/0
   recoverable      1       1             0           0/0 (n/a)                0                0                  0      0/1
   futile           0       0             0           0/0 (n/a)                0                0                  0      0/0
   not-a-place      0       0             0           0/0 (n/a)                0                0                  0      0/0

   `place-graded` counts only cases whose true places are ruled on AND whose lookups all ran.
   For `futile` and `not-a-place` the true place set is empty by the definition of the class,
   so any place offered on one of those posts is wrong — no place names have to be typed.

   WRONG PLACES OFFERED:  0 pre-selected   <- must be 0
                          0 shortlisted   <- a ranking annoyance, not a lie

   postIntent accuracy:  0/1 (0%)
     on explicit `expectedIntent` labels: 0/0   on labels DERIVED from the class: 0/1
     The derived half rests on INTENT_FOR_CLASS, a documented default and not a hand label.
     Where it disagrees with the owner, set `expectedIntent` on that row.
   confusion (expected → got):
     place_recommendation   {"not_a_place":1}

   ESCALATION GATE — positive class "recoverable" (the only class a transcript could rescue)
   gate                                                    tp  fp  fn  tn   prec  recall
   G1 zero-candidates                                      1   0   0   0   1.00  1.00
   G2 zero-candidates AND postIntent=place_recommendation  0   0   1   0    n/a  0.00
   G2 is G1 with `postIntent` added. The difference between the two rows IS the value of
   that field: if G2 does not beat G1 on precision, the field is not earning its tokens.

   SPEND THIS RUN
     model calls:      0  (the grader never calls the model; the seeder does)
     provider lookups: 0 live, 0 replayed from disk
     estimated cost:   $0.000 at the DOCUMENTED Text Search (Pro) list price of ~$32/1,000 (06 §11).
                       The first 5,000 lookups a month are free, so the real marginal cost
                       of this run is $0 until that tier is gone.
     model cost:       NOT PRICED. `integrations/llm/cost.ts` carries no verified per-token
                       price for the shipped Gemini model and logs `costModel: "unmeasured"`.
                       Token counts are in the seeded extraction fixtures.

----------------------------------------------------------------------------------------------------
SAMPLE: e7 — the BORROWED 16-post web-search set (docs/evidence/tiktok/07-caption-content-scoring.md).
        `07` documents its own bias: found via web search, skewed to indexed/high-reach
        posts. Every number below is a property of that sample, not of the product.
----------------------------------------------------------------------------------------------------

   !! NOT YET A MEASUREMENT: 10 adjudicated case(s) in this sample. The document this
      corpus replaces had n=16 and a wide confidence interval; a smaller n does not fix that.

   cases in this sample: 10

   PER CLASS
   class          posts  graded  place-graded  yielded>=1 correct   wrong preselect  wrong shortlist  extraction misses  intent
   sufficient       3       3             0           0/0 (n/a)                0                0                  0      3/3
   recoverable      4       4             3            0/3 (0%)                0                0                  2      4/4
   futile           2       2             2            0/2 (0%)                0                0                  0      1/2
   not-a-place      1       1             1            0/1 (0%)                0                0                  0      1/1

   `place-graded` counts only cases whose true places are ruled on AND whose lookups all ran.
   For `futile` and `not-a-place` the true place set is empty by the definition of the class,
   so any place offered on one of those posts is wrong — no place names have to be typed.

   WRONG PLACES OFFERED:  0 pre-selected   <- must be 0
                          0 shortlisted   <- a ranking annoyance, not a lie

   postIntent accuracy:  9/10 (90%)
     on explicit `expectedIntent` labels: 1/1   on labels DERIVED from the class: 8/9
     The derived half rests on INTENT_FOR_CLASS, a documented default and not a hand label.
     Where it disagrees with the owner, set `expectedIntent` on that row.
   confusion (expected → got):
     place_recommendation   {"place_recommendation":6}
     place_question         {"place_question":2,"place_recommendation":1}
     not_a_place            {"not_a_place":1}

   ESCALATION GATE — positive class "recoverable" (the only class a transcript could rescue)
   gate                                                    tp  fp  fn  tn   prec  recall
   G1 zero-candidates                                      4   3   0   3   0.57  1.00
   G2 zero-candidates AND postIntent=place_recommendation  3   1   1   5   0.75  0.75
   G2 is G1 with `postIntent` added. The difference between the two rows IS the value of
   that field: if G2 does not beat G1 on precision, the field is not earning its tokens.

   SPEND THIS RUN
     model calls:      0  (the grader never calls the model; the seeder does)
     provider lookups: 0 live, 0 replayed from disk
     estimated cost:   $0.000 at the DOCUMENTED Text Search (Pro) list price of ~$32/1,000 (06 §11).
                       The first 5,000 lookups a month are free, so the real marginal cost
                       of this run is $0 until that tier is gone.
     model cost:       NOT PRICED. `integrations/llm/cost.ts` carries no verified per-token
                       price for the shipped Gemini model and logs `costModel: "unmeasured"`.
                       Token counts are in the seeded extraction fixtures.
====================================================================================================
